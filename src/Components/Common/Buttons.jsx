import React from 'react'

  const Buttons = (props) => {

  
  return (
    <button className="  bg-sky-500 text-white rounded-none ...">
    {props.name}
    </button>
    
  )
  }

export default Buttons;